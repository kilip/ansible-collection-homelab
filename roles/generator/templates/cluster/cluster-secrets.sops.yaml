# yamllint disable
apiVersion: v1
kind: Secret
metadata:
  name: cluster-secrets
  namespace: flux-system
stringData:
  SECRET_DOMAIN: {{ homelab_cloudflare_domain }}
  SECRET_CLOUDFLARE_EMAIL: {{ homelab_cloudflare_email}}
